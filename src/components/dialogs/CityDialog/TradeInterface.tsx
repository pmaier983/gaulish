import { type InterfaceProps } from "~/components/dialogs/CityDialog"

interface TradeInterfaceProps extends InterfaceProps {}

export const TradeInterface = ({
  setCityDialogInterface,
}: TradeInterfaceProps) => {
  return <div>Trade</div>
}
